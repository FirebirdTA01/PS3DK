#ifndef __PS3DK_CELL_PAD_LIBPAD_H__
#define __PS3DK_CELL_PAD_LIBPAD_H__

/*
 * Code written against the split libpad include layout expects
 * <cell/pad/libpad.h>.  The independent SDK keeps the same ABI declarations in
 * the consolidated <cell/pad.h>; this header preserves that public path.
 */
#include <cell/pad.h>

#endif /* __PS3DK_CELL_PAD_LIBPAD_H__ */
