void main(float4 c : COLOR0, out float4 o : COLOR)
{
    float4 x = saturate(c.wzyx * float4(0.75, 0.5, 0.625, 0.875) + float4(0.125, 0.25, 0.0, 0.0625)) * 2.0 - 1.0;
    float4 a = asin(x.zwxy);
    float4 b = acos(x.yxwz);
    o = float4(a.w, b.z, a.y, b.x);
}
