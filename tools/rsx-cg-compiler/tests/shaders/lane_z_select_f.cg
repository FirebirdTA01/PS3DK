// FP ternary select using .z — SGT SRC0 swizzle (Z,Z,Z,Z).  See
// lane_y_select_f.cg for the shape.
void main(
    float4 vcol      : COLOR,
    uniform float4 a,
    uniform float4 b,
    out float4 color : COLOR)
{
    color = (vcol.z > 0.5f) ? a : b;
}
