// Control: plain scalar assignment reaches Select without VecInsert.
float4 main(float4 a : TEXCOORD0, uniform float enabled) : COLOR
{
    float s = a.x * 0.5;
    if (enabled)
        s = a.y;
    return float4(s, 2, 3, 4);
}
