// A then-only local shadowing the global dies before the join: the read after is the uniform.
float4 G;
float4 main(float4 p : TEXCOORD0) : COLOR { if (p.x > 0.0) { float4 G = p * 2.0; } return G; }
