// An assignment to an OUTER name inside a block survives the block (2*p), while the block-local dies.
float4 G;
float4 main(float4 p : TEXCOORD0) : COLOR { G = p; { float4 t = p * 2.0; G = t; } return G; }
