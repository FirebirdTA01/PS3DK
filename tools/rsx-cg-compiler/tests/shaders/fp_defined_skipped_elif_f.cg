// t_9e90fb38.  Measured against sce-cgc 475.
// The same again, in an #elif of a group whose parent is already skipped -
// the arm is unreachable twice over and still must not be evaluated.
#define V 1.0
#if 0
#if 0
#elif defined(
#endif
#endif
float4 main() : COLOR { return V; }
