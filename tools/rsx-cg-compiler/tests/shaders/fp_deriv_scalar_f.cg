void main(float2 uv : TEXCOORD0, out float4 o : COLOR)
{
    float pattern = uv.x * 0.5 + uv.y;
    float dx = ddx(pattern);
    float dy = ddy(pattern);
    o = float4(abs(dx), abs(dy), 0.0, 1.0);
}
