// t_36492ad8 commit 1.  REFUSE: A FILE-SCOPE INITIALISER IS A REACHABILITY ROOT.  `static float g =
// f(0.25);` reaches f's body even though the entry never mentions f, and
// the reference name-checks it there (C1008).  Rooting the walk only at
// the entry dropped the finding AND skipped lowering f (review: codex).
float f(float t) { return missing; }
static float g = f(0.25);
float4 main(float4 t : TEXCOORD0) : COLOR { return t; }
