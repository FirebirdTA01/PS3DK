void main(float4 position : POSITION, uniform float4x4 mvp, uniform float3 lightPos, out float4 oPos : POSITION, out float3 oL : TEXCOORD0)
{
    oPos = mul(mvp, position);
    oL = lightPos - float3(position);
}
