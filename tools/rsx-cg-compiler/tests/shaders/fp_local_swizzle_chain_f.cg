// t_6be25fd4: three source aliases must compose to a.wxzy.
// Scale and bias keep the varying lanes away from colour clamping.
void main(float4 a : TEXCOORD0, out float4 o : COLOR)
{
    float4 b = a.wzyx;
    float4 c = b.yzxw;
    o = c.zwxy * 0.25 + 0.375;
}
