// An outer struct FIELD written inside a block survives (a binding site with no DeclStmt).
struct S { float4 f; float4 g; };
float4 main(float4 p : TEXCOORD0) : COLOR { S s; s.f = p; s.g = p; { s.f = p * 2.0; } return s.f + s.g; }
