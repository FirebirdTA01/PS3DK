// t_9e90fb38.  Measured against sce-cgc 475.
// EVERY DOOR INTO A GROUP HAS TO SUPPRESS IT, not just #if.  My first
// skip fix covered processIf and left processIfdef taking everActive
// from its own predicate and processElif evaluating before it read the
// parent, so this shape walked straight into the #elif expression and
// refused where the reference accepts (found by codex on the first
// review build, not by me).
#define V 1.0
#if 0
#ifdef NEVER
#elif defined(
#endif
#endif
float4 main() : COLOR { return V; }
