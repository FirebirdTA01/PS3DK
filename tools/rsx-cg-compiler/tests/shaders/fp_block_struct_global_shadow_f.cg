// A block-local S s shadows the GLOBAL struct s (no helper): after the block s.f is the global's field (the uniform).
struct S { float4 f; };
S s;
float4 main(float4 p : TEXCOORD0) : COLOR { { S s; s.f = p * 3.0; } return s.f; }
