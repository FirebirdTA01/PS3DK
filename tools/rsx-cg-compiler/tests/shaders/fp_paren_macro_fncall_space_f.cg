// t_7cc742ed.  Measured against sce-cgc 475.
// A SPACE AT THE CALL is still a call - the rule is about the DEFINITION
// only.  Must equal fp_paren_macro_fn_f.
#define K(x) ((x)*2.0)
float4 main(float4 t : TEXCOORD0) : COLOR { return t * K (0.5); }
