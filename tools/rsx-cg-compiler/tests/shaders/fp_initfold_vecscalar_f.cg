// t_10dc2936: arithmetic in a file-scope initialiser folds the way the reference folds it.
const float2 D = float2(1.0f, 2.0f) * 0.5f;
float4 main(float2 t : TEXCOORD0) : COLOR { return float4(t * D, 0, 1); }
