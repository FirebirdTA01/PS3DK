// t_d594ccd9 witness header: an include guard that is never closed, exactly
// the shape of libretro's compat_macros.inc (three opens, two closes).
// The reference lets the open block continue in the includer.
#ifndef OPEN_GUARD_H
#define OPEN_GUARD_H
#define SAMPLER_T sampler2D
