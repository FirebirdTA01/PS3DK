uniform float4 u_scale;
uniform float4 u_a[2];
uniform float4 u_b[3];
void main(float4 pos : POSITION, float idx : TEXCOORD1, out float4 oPos : POSITION) { oPos = pos * u_scale + u_a[1] + u_b[int(idx)]; }
