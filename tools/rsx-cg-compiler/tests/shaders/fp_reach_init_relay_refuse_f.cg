// t_36492ad8 commit 1.  REFUSE: the same through a RELAY - initialiser calls h, h calls f, f has
// the bad name.  The root has to be transitive, not one level.
float f(float t) { return missing; }
float h(float t) { return f(t); }
static float g = h(0.25);
float4 main(float4 t : TEXCOORD0) : COLOR { return t; }
