// t_36492ad8 commit 1.  REFUSE: the same with a FILE-SCOPE variable as the callee.  The two scopes
// reach the symbol by different lookups, so one row does not cover both.
float missing;
float4 unused(float4 t) { return missing(t); }
float4 main(float4 t : TEXCOORD0) : COLOR { return t; }
