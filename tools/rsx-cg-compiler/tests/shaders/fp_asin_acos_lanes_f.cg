void main(float4 c : COLOR0, out float4 o : COLOR)
{
    float3 x = saturate(c.xyz * float3(0.75, 0.5, 0.625) + float3(0.125, 0.25, 0.0)) * 2.0 - 1.0;
    float3 a = asin(x);
    float3 b = acos(x);
    o = float4(a.y, b.z, (a + b).x, 1.0);
}
