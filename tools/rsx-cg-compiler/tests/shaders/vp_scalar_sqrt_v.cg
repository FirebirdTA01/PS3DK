void main(float4 position : POSITION,
          float val : TEXCOORD0,
          out float4 oPosition : POSITION,
          out float4 oResult : TEXCOORD0)
{
    oPosition = position;
    float r = sqrt(val);
    oResult = float4(r, r, r, r);
}
