// Preserving a shared float2 base must not define its absent z/w lanes.
void main(float4 p : POSITION, out float4 position : POSITION,
          out float2 uv : TEXCOORD0)
{
    position = p;
    uv.xy = p.xy * 0.25 + 0.5;
    uv.y = 0.75 - uv.y;
}
