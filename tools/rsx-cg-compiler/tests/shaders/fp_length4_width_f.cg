// t_71ad405e: length(float4) must include w with DP4.
void main(float4 c : TEXCOORD0, out float4 o : COLOR)
{
    float4 p = c * float4(0.125, 0.25, 0.5, 0.75);
    float l = length(p);
    o = float4(l, l, l, 1.0);
}
