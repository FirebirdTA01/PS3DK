void main(float4 position : POSITION,
          float4 val : TEXCOORD0,
          uniform float4 u_bounds,
          out float4 oPosition : POSITION,
          out float4 oResult : TEXCOORD0)
{
    oPosition = position;
    oResult = clamp(val, u_bounds.x, u_bounds.y);
}
