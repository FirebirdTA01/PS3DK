// The dot is exactly zero; the strictly-negative predicate must keep -N.
float4 main(float4 n : TEXCOORD0) : COLOR
{
    float4 r = faceforward(n, float4(1.0, 0.0, 0.0, 0.0),
                             float4(0.0, 1.0, 0.0, 0.0));
    return r * 0.25 + 0.5;
}
