// t_36492ad8 commit 1.  REFUSE: the same shadow in a REACHABLE body.  Pairs with the row above so
// neither "always report" nor "never report" satisfies both.
float4 f(float4 t) { return t * 2.0; }
float4 unused(float4 t) { float f = 1; return f(t); }
float4 main(float4 t : TEXCOORD0) : COLOR { return unused(t); }
