float4 B[1]; void gen(float4 p) { B[0] = p * 2; } float4 get() { return B[0]; } float4 main(float4 p : POSITION) : POSITION { B[0] = p; float4 B[1]; B[0] = p * 3; if (p.x > 0) gen(p); return get(); }
