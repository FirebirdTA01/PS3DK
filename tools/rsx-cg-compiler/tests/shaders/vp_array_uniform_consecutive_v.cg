uniform float4 u_bones[4];
void main(float4 pos : POSITION, float2 idx : TEXCOORD1, out float4 oPos : POSITION, out float4 oCol : COLOR0) { oPos = pos + u_bones[int(idx.x)]; oCol = u_bones[int(idx.y)]; }
