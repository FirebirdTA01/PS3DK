// Smallest vertex-program identity passthrough.  Used by the sce-cgc
// byte-diff harness to lock in bit-exact NV40 ucode emit.
struct VIN  { float4 pos : POSITION; };
struct VOUT { float4 pos : POSITION; };

VOUT main(VIN input)
{
    VOUT o;
    o.pos = input.pos;
    return o;
}
