// hello-ppu-cellgcm-blend — vertex program.
//
// MVP-transform + per-vertex RGBA passthrough.  Identical shape to
// the triangle sample's VP — just kept here for sample-locality.

void main(
    float3 in_position      : POSITION,
    float4 in_color         : COLOR,
    uniform float4x4 modelViewProj,
    out float4 out_position : POSITION,
    out float4 out_color    : COLOR)
{
    out_position = mul(modelViewProj, float4(in_position, 1.0f));
    out_color    = in_color;
}
